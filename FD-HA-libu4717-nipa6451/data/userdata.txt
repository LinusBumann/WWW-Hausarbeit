import { DB } from "https://deno.land/x/sqlite@v3.7.0/mod.ts";

const db = new DB("data/userdata.sqlite");

db.execute(`CREATE TABLE IF NOT EXISTS userdata (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    vorname TEXT,
    nachname TEXT,
    email TEXT,
    passwort TEXT
);
INSERT INTO userdata (vorname, nachname, email, passwort) 
VALUES 
('Nico', 'Paulsen', 'paulsen_nico@web.de', 'abcd1234');
`);
db.close();